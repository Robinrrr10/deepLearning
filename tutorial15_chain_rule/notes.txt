
Tutorial 15: Chain Rule | Deep Learning Tutorial 15 (Tensorflow2.0, Keras & Python)
-----------------------------------------------------------------------------------
Chain rule is part of differential calculus
Lets say we have home price prediction with samples which has squrefeet and bedrooms
price = w1 * x1 + w2 * x2 + bias
each square feet w1 and number of bedrooms w2 is passed into the formula to get the price
y = nEi=0 wixi + b
each data will be passed and we will get the y predicted value
Now we will compare with y truth value and we will calculate the error
error1 = y - y1 
squared error1
Now we will get total error
And calculate the mean
total errors = err1 + err2 + .... + err6
mean squre error = err1 + err2 + .... + err6 / 6
loss = mean squared error

y - y = e2 to calculate the loss

w1 = w1 - something
w1 = w1 - learning rate * a/aw1
w2 = w2 - learning rate * a/aw2
b = b - learning rate * a/b

We will take w1, w2 and b as 1 then we will adjust the weight
And we will pass to the next iteration
Until we get the optimal value or global minima we will do the same again and again for each iteration

aL/ae = 2e
ae/ey = -1
ay/aw1 = area

aL/aw1 = ?
aL/aw1 = aL/ae * ae/ay^ * ay^/w1
aL/aw1 = -2e * area

This is chain rule


Lets say we have age, education, income and saving for insurance data set which decides whether the person will buy insurance or not. 
Lets say this has multi layer neural network.
Which has middle layer where age and education decides awareness. (income and saving may not play major role to decide awareness)
Where another middle layer where income and saving decides affordability. (age and education may not play major role to decide affordability)
Then awareness and affordability decides the output layer whether the person will buy insurance or not

We can simplify in neural network graph. We can call this as computation graph
We will be finding how much it changes based on given changes in input
This helps to comes up with optimal value
Given the change in x how much the y changes


From each smaller equation, this will find the bigger equation
lets say a and b are input layer or x and c and d are input layer of y
x and y are the middley layer. And the output layer is z
Lets sat x=a^2 + 7b and y=c^3 + d
then z = 4x + 3y
az/ax = 4
ax/aa = 2a
now az/aa = az/ax * ax/aa
 = 4 * 2a
 = 8a
ax/ab = 7
az/ab  = az/ax * ax/ab
 = 4 * 7
 = 28

This is how the bigger forumla is derived from the smaller formula in neural network

Please check the images of this tutorial15 for more details about chain rule
--------------
